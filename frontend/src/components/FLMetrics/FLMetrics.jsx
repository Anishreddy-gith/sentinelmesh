// TODO: Chart FL training rounds, global AUC per round, epsilon budget consumed
export default function FLMetrics() {
  return (
    <div className="text-gray-500 text-sm text-center py-8">
      FL Metrics (TODO: connect to /api/federated/metrics)
    </div>
  );
}
